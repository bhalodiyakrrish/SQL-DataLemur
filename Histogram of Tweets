WITH tweet_bracket AS (
  SELECT
    user_id,
    COUNT(tweet_id) AS tweet_counts
  FROM tweets
  WHERE YEAR(tweet_date) = 2022
  GROUP BY user_id
)
SELECT
  tweet_counts AS tweet_bucket,
  COUNT(user_id) AS users_num
FROM tweet_bracket
GROUP BY tweet_counts;
