SELECT
  order_id AS corrected_order_id,
  CASE 
    WHEN order_id % 2 != 0 THEN LEAD(item,1,item) OVER(ORDER BY order_id)
    WHEN order_id % 2 = 0 THEN LAG(item,1) OVER(ORDER BY order_id)
  END AS item
FROM orders;
