SELECT
  COUNT(policy_holder_id) AS policy_holder_count
FROM (
SELECT
  policy_holder_id,
  COUNT(DISTINCT case_id) AS total_calls
FROM callers
GROUP BY policy_holder_id
HAVING COUNT(DISTINCT case_id) >= 3) AS t1
